# === GPIO & timing ===
RELAY_PIN=17
PULSE_MS=700
LOCKOUT_SEC=2.0
ACTIVE_HIGH=true

# === Listener ===
LISTEN_HOST=0.0.0.0
LISTEN_PORT=8080

# === Security ===
SHARED_SECRET=change_me_to_a_long_random_string
